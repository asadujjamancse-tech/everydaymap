import React from 'react';

// POI markers are now rendered inside LeafletMap via react-leaflet Marker components.
// This stub keeps existing imports in Map.tsx working without errors.
export const POILayer: React.FC = () => null;
